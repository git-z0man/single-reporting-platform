host:        sk.cra-srp.enisa.europa.eu
country:     Slowakei (SK)
csirt:       SK-CERT (NBÚ)
contact:     http://www.sk-cert.sk
status:      verifiziert
verified_on: 2026-09-07

signal:
  ENISA, "List of CSIRTs Designated as Coordinators" (Stand 04/09/2026)
  https://www.enisa.europa.eu/topics/product-security/single-reporting-platform-srp/list-of-csirts-designated-as-coordinators

  The mapping is taken from ENISA's own published list of CSIRTs
  designated as coordinators under the CRA, not inferred from the host
  itself. The host is still dark (PROVISIONED), so no signal could be
  read from it; this source is authoritative regardless.

go_live_check_2026_09_11T11_36_58Z:
  Host LIVE (HTTP 307, server: srp — genuine SRP application, not the WEDOS
  edge decoy). Sign-in redirect: client_id=srp-ar-fe-sk, kc_idp_hint=eu-login
  — matches this host's own ISO code exactly, no cross-wiring to another
  country's client found. Structural (routing) confirmation only: full CSIRT
  branding/imprint sits behind EU Login authentication and was not reachable
  from public, pre-login signals, so this does not by itself confirm the
  CSIRT named above — it confirms the platform routes this host to its own,
  correctly-scoped country instance.
